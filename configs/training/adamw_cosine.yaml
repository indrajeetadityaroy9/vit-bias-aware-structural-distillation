# AdamW optimizer + cosine schedule defaults
training:
  optimizer: adamw
  scheduler: cosine
  weight_decay: 0.05
  warmup_epochs: 10
  gradient_clip_val: 1.0
  label_smoothing: 0.1
  use_bf16: true
  use_compile: true
  compile_mode: max-autotune
  use_fused_optimizer: true
  use_tf32: true
