run:
  name: basd_cifar100

data:
  dataset: uoft-cs/cifar100
  batch_size: 128
  channel_stats:
    mean: [0.5071, 0.4867, 0.4408]
    std: [0.2675, 0.2565, 0.2761]

model:
  student_preset: deit_tiny
  vit:
    img_size: 32
    patch_size: 4

training:
  num_epochs: 200
  learning_rate: 5e-4
